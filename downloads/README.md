# Place the three downloadable CSV files here

```text
tdb_core_interaction_table.csv
gene_alias_table.csv
biogrid_source_table.csv
```

These are public download copies. The website search uses the JSON files in `data/`; the CSV files are provided for reuse, transparency and manual inspection.
